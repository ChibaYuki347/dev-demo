# Runtime dependencies for the demo Function.
# Pin versions for reproducibility.
azure-functions==1.21.3

# Test-time deps — installed in the same file for the demo. In production they
# would live in requirements-dev.txt.
pytest==8.3.4
pytest-cov==6.0.0
