# Dev-time dependencies — installed normally into the system Python for pytest.
# Runtime-only deps live in requirements-runtime.txt and are installed with
# `pip install --target=".python_packages/lib/site-packages"` to ride along
# with the deploy package.

-r requirements-runtime.txt
pytest==9.0.3
pytest-cov==6.0.0

